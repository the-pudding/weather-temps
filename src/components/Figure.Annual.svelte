<script>
  import { activeSlide } from "$stores/misc.js";
  // const extentDayRecords = extent(data, (d) => d.day);

  // const justRecords = data.filter((d) => d.rank === 0);
  // justRecords.sort((a, b) => ascending(a.day, b.day));
</script>
